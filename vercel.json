{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "buildCommand": "pnpm lint && next build",
  "installCommand": "pnpm install --frozen-lockfile",
  "framework": "nextjs",
  "git": {
    "deploymentEnabled": {
      "main": true,
      "develop": true
    }
  }
}
